<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .follower-info {
        background: #f8f9fa;
        border-radius: 8px;
        padding: 20px;
        margin: 20px 0;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 4px;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <h1>You have a new follower! 🎉</h1>
    
    <div class="follower-info">
      <p>
        <strong><%= @follower.display_name %></strong> is now following you.
      </p>
      
      <% if @follower.blog_posts.published.exists? %>
        <p>
          They have published <%= pluralize(@follower.blog_posts.published.count, 'post') %>
          and made <%= pluralize(@follower.comments.count, 'comment') %>.
        </p>
      <% end %>
    </div>
    
    <p>View their profile to learn more:</p>
    
    <%= link_to "View Profile", profile_url(@follower), class: "button" %>
    
    <p>
      Best regards,<br>
      The Blog Team
    </p>
  </body>
</html>
